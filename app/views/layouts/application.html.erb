<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Parejas Game" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload" %>
    <script src="https://cdn.tailwindcss.com"></script>
    <%= yield :head %>
    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-gray-100">
    <!-- Navbar fijo con color dinámico -->
    <nav class="fixed top-0 left-0 right-0 z-50 py-4 px-6 flex justify-between items-center shadow-md"
         style="background-color: <%= @game&.header_color || '#ffffff' %>">
      <div class="flex items-center">
        <% if @game&.logo&.attached? %>
          <div class="h-10 w-10 mr-2 bg-white bg-opacity-20 rounded flex items-center justify-center">
            <%= image_tag @game.logo.variant(resize_to_limit: [40, 40]), 
                class: "max-h-full max-w-full object-contain" %>
          </div>
        <% else %>
          <h1 class="text-xl font-bold" style="color: <%= @game&.text_color || '#1f2937' %>">
            Parejas Game
          </h1>
        <% end %>
      </div>

      <!-- Selector de idioma -->
      <div>
        <%= link_to "🇪🇸 Español", change_locale_path("es"), class: "hover:opacity-80", style: "color: #{@game&.text_color || '#3b82f6'}" %> |
        <%= link_to "🇺🇸 English", change_locale_path("en"), class: "hover:opacity-80", style: "color: #{@game&.text_color || '#3b82f6'}" %>
      </div>
    </nav>

    <!-- Espacio para el navbar fijo -->
    <div class="pt-20"></div>

    <main class="container mx-auto px-5 pb-10">
      <%= yield %>
    </main>
  </body>
</html>

